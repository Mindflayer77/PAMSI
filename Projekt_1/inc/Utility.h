#pragma once
#include "SLPQueue.h"
#include <vector>
#include <list>
#include <string>
#include <set>

// bool outData(const std::vector<std::string>& data, const int& size);
// bool inData(std::vector<std::string>& data);
bool contains(const std::set<int>& genNum, const int& num);